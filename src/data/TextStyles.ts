import { Fonts } from "../assets/Fonts";

export default class TextStyles {
    public static BOLD = {
        font: "15px " + Fonts.BOLD
    };
    public static TEXT = {
        font: "15px " + Fonts.TEXT
    };
}